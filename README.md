OTT Store - Netflix Dark UI Demo
Replace placeholder images in static/img with real logos (keep filenames the same).
Set environment variables on Render as per .env.example before deployment.
Admin: username=admin password=12345 (or set via env)
